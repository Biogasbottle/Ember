# Domain Docs

This project uses a **single-context** layout:

- `CONTEXT.md` at the repo root — domain language and stable concepts
- `docs/adr/` at the repo root — architectural decisions

All skills read from these two locations.
