# Triage Labels

This project uses a minimal set of two triage labels:

| Role | Label |
|---|---|
| Fully specified, AFK-ready | `ready-for-agent` |
| Will not action | `wontfix` |

The labels `needs-triage`, `needs-info`, and `ready-for-human` are intentionally not used.
